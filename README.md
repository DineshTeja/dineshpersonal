# Dinesh Vasireddy

A framework-free personal website built with semantic HTML and a single CSS file. Vite is used only for local serving and packaging the static files.

## Local development

```bash
yarn install
yarn dev
```

## Production build

```bash
yarn build
```

The generated site is written to `dist/client`, with a small static-asset worker in `dist/server` for hosting.
